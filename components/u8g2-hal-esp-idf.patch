diff --git a/CMakeLists.txt b/CMakeLists.txt
index cfba22b..2fe2936 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,4 +1,4 @@
 file(GLOB_RECURSE SOURCES src/*.c)
 idf_component_register(SRCS ${SOURCES}
                        INCLUDE_DIRS "include"
-                       REQUIRES driver freertos u8g2)
\ No newline at end of file
+                       REQUIRES driver esp_driver_i2c esp_driver_spi esp_driver_gpio freertos u8g2)
\ No newline at end of file
diff --git a/include/u8g2_esp32_hal.h b/include/u8g2_esp32_hal.h
index aa55996..386edb5 100644
--- a/include/u8g2_esp32_hal.h
+++ b/include/u8g2_esp32_hal.h
@@ -12,16 +12,12 @@
 #include "u8g2.h"
 
 #include "driver/gpio.h"
-#include "driver/i2c.h"
+#include "driver/i2c_master.h"
+#include "driver/i2c_types.h"
 #include "driver/spi_master.h"
 
 #define U8G2_ESP32_HAL_UNDEFINED GPIO_NUM_NC
-
-#if SOC_I2C_NUM > 1
-#define I2C_MASTER_NUM I2C_NUM_1     //  I2C port number for master dev
-#else
-#define I2C_MASTER_NUM I2C_NUM_0     //  I2C port number for master dev
-#endif
+#define U8G2_ESP32_HAL_UNDEFINED_I2C_BUS NULL
 
 #define I2C_MASTER_TX_BUF_DISABLE 0  //  I2C master do not need buffer
 #define I2C_MASTER_RX_BUF_DISABLE 0  //  I2C master do not need buffer
@@ -29,6 +25,8 @@
 #define ACK_CHECK_EN 0x1             //  I2C master will check ack from slave
 #define ACK_CHECK_DIS 0x0  //  I2C master will not check ack from slave
 
+#define I2C_STAGING_BUFFER_SIZE 4096
+
 /** @public
  * HAL configuration structure.
  */
@@ -45,10 +43,8 @@ typedef struct {
     } spi;
     /* I2C settings. */
     struct {
-      /* GPIO num for I2C data. */
-      gpio_num_t sda;
-      /* GPIO num for I2C clock. */
-      gpio_num_t scl;
+      /* Active I2C master bus handle */
+      i2c_master_bus_handle_t i2c_bus_handle;
     } i2c;
   } bus;
   /* GPIO num for reset. */
@@ -57,16 +53,20 @@ typedef struct {
   gpio_num_t dc;
 } u8g2_esp32_hal_t;
 
+typedef struct {
+  uint8_t buf[I2C_STAGING_BUFFER_SIZE];
+  size_t len;
+} u8g2_esp32_hal_i2c_staging;
+
 /**
  * Construct a default HAL configuration with all fields undefined.
  */
-#define U8G2_ESP32_HAL_DEFAULT                                        \
-  {                                                                   \
-    .bus = {.spi = {.clk = U8G2_ESP32_HAL_UNDEFINED,                  \
-                    .mosi = U8G2_ESP32_HAL_UNDEFINED,                 \
-                    .cs = U8G2_ESP32_HAL_UNDEFINED}},                 \
-    .reset = U8G2_ESP32_HAL_UNDEFINED, .dc = U8G2_ESP32_HAL_UNDEFINED \
-  }
+#define U8G2_ESP32_HAL_DEFAULT                       \
+  {.bus = {.spi = {.clk = U8G2_ESP32_HAL_UNDEFINED,  \
+                   .mosi = U8G2_ESP32_HAL_UNDEFINED, \
+                   .cs = U8G2_ESP32_HAL_UNDEFINED}}, \
+   .reset = U8G2_ESP32_HAL_UNDEFINED,                \
+   .dc = U8G2_ESP32_HAL_UNDEFINED}
 
 /**
  * Initialize the HAL with the given configuration.
@@ -74,6 +74,10 @@ typedef struct {
  * @see u8g2_esp32_hal_t
  * @see U8G2_ESP32_HAL_DEFAULT
  */
+#ifdef __cplusplus
+extern "C" {
+#endif
+
 void u8g2_esp32_hal_init(u8g2_esp32_hal_t u8g2_esp32_hal_param);
 uint8_t u8g2_esp32_spi_byte_cb(u8x8_t* u8x8,
                                uint8_t msg,
@@ -87,6 +91,11 @@ uint8_t u8g2_esp32_gpio_and_delay_cb(u8x8_t* u8x8,
                                      uint8_t msg,
                                      uint8_t arg_int,
                                      void* arg_ptr);
+
+#ifdef __cplusplus
+}
+#endif
+
 #endif /* U8G2_ESP32_HAL_H_ */
 
 #endif
\ No newline at end of file
diff --git a/src/u8g2_esp32_hal.c b/src/u8g2_esp32_hal.c
index c76314f..e2b488d 100644
--- a/src/u8g2_esp32_hal.c
+++ b/src/u8g2_esp32_hal.c
@@ -10,13 +10,14 @@
 #include "u8g2_esp32_hal.h"
 
 static const char* TAG = "u8g2_hal";
-static const unsigned int I2C_TIMEOUT_MS = 1000;
+// static const unsigned int I2C_TIMEOUT_MS = 1000;
 
-static spi_device_handle_t handle_spi;   // SPI handle.
-static i2c_cmd_handle_t handle_i2c;      // I2C handle.
-static u8g2_esp32_hal_t u8g2_esp32_hal;  // HAL state data.
+static spi_device_handle_t handle_spi;          // SPI handle.
+static i2c_master_dev_handle_t handle_i2c_dev;  // I2C device handle.
+static u8g2_esp32_hal_t u8g2_esp32_hal;         // HAL state data.
+static u8g2_esp32_hal_i2c_staging i2c_staging_buffer;
 
-#define HOST    SPI2_HOST
+#define HOST SPI2_HOST
 
 #undef ESP_ERROR_CHECK
 #define ESP_ERROR_CHECK(x)                   \
@@ -97,8 +98,8 @@ uint8_t u8g2_esp32_spi_byte_cb(u8x8_t* u8x8,
       trans_desc.rxlength = 0;
       trans_desc.tx_buffer = arg_ptr;
       trans_desc.rx_buffer = NULL;
-      // trans_desc.override_freq_hz = 0; // this param does not exist prior to ESP-IDF 5.5.0
-      // ESP_LOGI(TAG, "... Transmitting %d bytes.", arg_int);
+      // trans_desc.override_freq_hz = 0; // this param does not exist prior to
+      // ESP-IDF 5.5.0 ESP_LOGI(TAG, "... Transmitting %d bytes.", arg_int);
       ESP_ERROR_CHECK(spi_device_transmit(handle_spi, &trans_desc));
       break;
     }
@@ -114,7 +115,7 @@ uint8_t u8g2_esp32_i2c_byte_cb(u8x8_t* u8x8,
                                uint8_t msg,
                                uint8_t arg_int,
                                void* arg_ptr) {
-  ESP_LOGD(TAG, "i2c_cb: Received a msg: %d, arg_int: %d, arg_ptr: %p", msg,
+  ESP_LOGV(TAG, "i2c_cb: Received a msg: %d, arg_int: %d, arg_ptr: %p", msg,
            arg_int, arg_ptr);
 
   switch (msg) {
@@ -126,59 +127,63 @@ uint8_t u8g2_esp32_i2c_byte_cb(u8x8_t* u8x8,
     }
 
     case U8X8_MSG_BYTE_INIT: {
-      if (u8g2_esp32_hal.bus.i2c.sda == U8G2_ESP32_HAL_UNDEFINED ||
-          u8g2_esp32_hal.bus.i2c.scl == U8G2_ESP32_HAL_UNDEFINED) {
+      ESP_LOGI(TAG, "BYTE_INIT called, bus_handle: %p, dev_handle before: %p",
+               (void*)u8g2_esp32_hal.bus.i2c.i2c_bus_handle,
+               (void*)handle_i2c_dev);
+      if (u8g2_esp32_hal.bus.i2c.i2c_bus_handle ==
+          U8G2_ESP32_HAL_UNDEFINED_I2C_BUS) {
         break;
       }
 
-      i2c_config_t conf = {0};
-      conf.mode = I2C_MODE_MASTER;
-      ESP_LOGI(TAG, "sda_io_num %d", u8g2_esp32_hal.bus.i2c.sda);
-      conf.sda_io_num = u8g2_esp32_hal.bus.i2c.sda;
-      conf.sda_pullup_en = GPIO_PULLUP_ENABLE;
-      ESP_LOGI(TAG, "scl_io_num %d", u8g2_esp32_hal.bus.i2c.scl);
-      conf.scl_io_num = u8g2_esp32_hal.bus.i2c.scl;
-      conf.scl_pullup_en = GPIO_PULLUP_ENABLE;
-      ESP_LOGI(TAG, "clk_speed %d", I2C_MASTER_FREQ_HZ);
-      conf.master.clk_speed = I2C_MASTER_FREQ_HZ;
-      ESP_LOGI(TAG, "i2c_param_config %d", conf.mode);
-      ESP_ERROR_CHECK(i2c_param_config(I2C_MASTER_NUM, &conf));
-      ESP_LOGI(TAG, "i2c_driver_install %d", I2C_MASTER_NUM);
-      ESP_ERROR_CHECK(i2c_driver_install(I2C_MASTER_NUM, conf.mode,
-                                         I2C_MASTER_RX_BUF_DISABLE,
-                                         I2C_MASTER_TX_BUF_DISABLE, 0));
+      uint16_t device_addr = (u8x8_GetI2CAddress(u8x8) >> 1);
+
+      // TODO: Guard against multiple init calls
+      ESP_LOGI(TAG, "Creating I2C Device Config");
+
+      i2c_device_config_t i2c_device_config = {
+          .dev_addr_length = I2C_ADDR_BIT_LEN_7,
+          .device_address = device_addr,
+          .scl_speed_hz = I2C_MASTER_FREQ_HZ,
+          .scl_wait_us = 0,
+          .flags.disable_ack_check = false};
+      ESP_ERROR_CHECK(
+          i2c_master_bus_add_device(u8g2_esp32_hal.bus.i2c.i2c_bus_handle,
+                                    &i2c_device_config, &handle_i2c_dev))
+      ESP_LOGI(TAG, "BYTE_INIT done, dev_handle after: %p",
+               (void*)handle_i2c_dev);
       break;
     }
 
     case U8X8_MSG_BYTE_SEND: {
-      uint8_t* data_ptr = (uint8_t*)arg_ptr;
-      ESP_LOG_BUFFER_HEXDUMP(TAG, data_ptr, arg_int, ESP_LOG_VERBOSE);
-
-      while (arg_int > 0) {
-        ESP_ERROR_CHECK(
-            i2c_master_write_byte(handle_i2c, *data_ptr, ACK_CHECK_EN));
-        data_ptr++;
-        arg_int--;
-      }
+      ESP_LOGV(TAG, "Adding %d bytes to i2c buffer from addr %p, len: %d -> %d",
+               arg_int, arg_ptr, i2c_staging_buffer.len,
+               i2c_staging_buffer.len + arg_int);
+      memcpy(i2c_staging_buffer.buf + i2c_staging_buffer.len, arg_ptr, arg_int);
+      i2c_staging_buffer.len += arg_int;
       break;
     }
 
     case U8X8_MSG_BYTE_START_TRANSFER: {
       uint8_t i2c_address = u8x8_GetI2CAddress(u8x8);
-      handle_i2c = i2c_cmd_link_create();
-      ESP_LOGD(TAG, "Start I2C transfer to %02X.", i2c_address >> 1);
-      ESP_ERROR_CHECK(i2c_master_start(handle_i2c));
-      ESP_ERROR_CHECK(i2c_master_write_byte(
-          handle_i2c, i2c_address | I2C_MASTER_WRITE, ACK_CHECK_EN));
+      ESP_LOGV(TAG, "Start I2C transfer to %02X.", i2c_address);
+
+      i2c_staging_buffer.len = 0;
+
       break;
     }
 
     case U8X8_MSG_BYTE_END_TRANSFER: {
-      ESP_LOGD(TAG, "End I2C transfer.");
-      ESP_ERROR_CHECK(i2c_master_stop(handle_i2c));
-      ESP_ERROR_CHECK(i2c_master_cmd_begin(I2C_MASTER_NUM, handle_i2c,
-                                           pdMS_TO_TICKS(I2C_TIMEOUT_MS)));
-      i2c_cmd_link_delete(handle_i2c);
+      ESP_LOGV(TAG, "End/send I2C buffer to handle_i2c_dev: %p, len: %d",
+               (void*)handle_i2c_dev, i2c_staging_buffer.len);
+      ESP_LOG_BUFFER_HEXDUMP(TAG, i2c_staging_buffer.buf,
+                             i2c_staging_buffer.len, ESP_LOG_VERBOSE);
+
+      ESP_ERROR_CHECK(i2c_master_transmit(handle_i2c_dev,
+                                          i2c_staging_buffer.buf,
+                                          i2c_staging_buffer.len, 1000));
+
+      i2c_staging_buffer.len = 0;
+
       break;
     }
   }
@@ -193,7 +198,7 @@ uint8_t u8g2_esp32_gpio_and_delay_cb(u8x8_t* u8x8,
                                      uint8_t msg,
                                      uint8_t arg_int,
                                      void* arg_ptr) {
-  ESP_LOGD(TAG,
+  ESP_LOGV(TAG,
            "gpio_and_delay_cb: Received a msg: %d, arg_int: %d, arg_ptr: %p",
            msg, arg_int, arg_ptr);
 
@@ -239,17 +244,17 @@ uint8_t u8g2_esp32_gpio_and_delay_cb(u8x8_t* u8x8,
       break;
       // Set the Software I²C pin to the value passed in through arg_int.
     case U8X8_MSG_GPIO_I2C_CLOCK:
-      if (u8g2_esp32_hal.bus.i2c.scl != U8G2_ESP32_HAL_UNDEFINED) {
-        gpio_set_level(u8g2_esp32_hal.bus.i2c.scl, arg_int);
-        //				printf("%c",(arg_int==1?'C':'c'));
-      }
+      // if (u8g2_esp32_hal.bus.i2c.scl != U8G2_ESP32_HAL_UNDEFINED) {
+      //   gpio_set_level(u8g2_esp32_hal.bus.i2c.scl, arg_int);
+      //   //				printf("%c",(arg_int==1?'C':'c'));
+      // }
       break;
       // Set the Software I²C pin to the value passed in through arg_int.
     case U8X8_MSG_GPIO_I2C_DATA:
-      if (u8g2_esp32_hal.bus.i2c.sda != U8G2_ESP32_HAL_UNDEFINED) {
-        gpio_set_level(u8g2_esp32_hal.bus.i2c.sda, arg_int);
-        //				printf("%c",(arg_int==1?'D':'d'));
-      }
+      // if (u8g2_esp32_hal.bus.i2c.sda != U8G2_ESP32_HAL_UNDEFINED) {
+      //   gpio_set_level(u8g2_esp32_hal.bus.i2c.sda, arg_int);
+      //   //				printf("%c",(arg_int==1?'D':'d'));
+      // }
       break;
 
       // Delay for the number of milliseconds passed in through arg_int.
